#ifndef REDFASTGI_PACKING_INCLUDED
#define REDFASTGI_PACKING_INCLUDED

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

float3 unpackR11G11B10(uint packed)
{
    return float3((float)((packed      ) & 0x000007ff) / 2047.0f,
                  (float)((packed >> 11) & 0x000007ff) / 2047.0f,
                  (float)((packed >> 22) & 0x000003ff) / 1023.0f);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

uint packR11G11B10(float3 value)
{
    return (uint(saturate(value.x) * 2047 + 0.5f)) |
           (uint(saturate(value.y) * 2047 + 0.5f) << 11) |
           (uint(saturate(value.z) * 1023 + 0.5f) << 22);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

float4 unpackR8G8B8A8(uint packed)
{
    return float4((float)((packed      ) & 0xFF) / 255,
                  (float)((packed >>  8) & 0xFF) / 255,
                  (float)((packed >> 16) & 0xFF) / 255,
                  (float)((packed >> 24) & 0xFF) / 255);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

uint packR8G8B8A8(float4 value)
{
    return (uint(saturate(value.x) * 255 + 0.5f)      ) |
           (uint(saturate(value.y) * 255 + 0.5f) <<  8) |
           (uint(saturate(value.z) * 255 + 0.5f) << 16) |
           (uint(saturate(value.w) * 255 + 0.5f) << 24);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

// Based on the code in com.unity.render-pipelines.core/ShaderLibrary/Packing.hlsl

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

float packByte(uint i)
{
    return saturate(i * rcp((1u << 8) - 1u));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

uint unpackByte(float f)
{
    return (uint)(f * ((1u << 8) - 1u) + 0.5);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

uint3 packFloat2To888UInt(float2 f)
{
    uint2 i = (uint2)(f * 4095.5);
    uint2 hi = i >> 8;
    uint2 lo = i & 255;

    return uint3(lo, hi.x | (hi.y << 4));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

float3 packFloat2To888(float2 f)
{
    return packFloat2To888UInt(f) / 255.0;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

float2 unpack888UIntToFloat2(uint3 x)
{
    uint hi = x.z >> 4;
    uint lo = x.z & 15;
    uint2 cb = x.xy | uint2(lo << 8, hi << 8);

    return cb / 4095.0;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

float2 unpack888ToFloat2(float3 x)
{
    return unpack888UIntToFloat2((uint3)(x * 255.5));
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

float2 packNormalOctQuadEncode(float3 n)
{
    n *= rcp(max(dot(abs(n), 1.0), 1e-6));

    float t = saturate(-n.z);

    return n.xy + (n.xy >= 0.0 ? t : -t);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

float3 unpackNormalOctQuadEncode(float2 f)
{
    float3 n = float3(f.x, f.y, 1.0 - abs(f.x) - abs(f.y));

    float t = max(-n.z, 0.0);
    n.xy += n.xy >= 0.0 ? -t.xx : t.xx;

    return normalize(n);
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#endif